spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mariadb:10:///pharmacyRecommendation
    username: root
    password: 1234

pharmacy:
  recommendation:
    base:
      url: http://localhost/dir/

kakao:
  rest:
    api:
      key: ${KAKAO_REST_API_KEY}